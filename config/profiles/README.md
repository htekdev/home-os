# Example custom profile
# Place YAML files here and they'll be loaded alongside built-in profiles.
#
# Required fields: name, systemPrompt
# Optional: description, defaultModel, cwd, bootstrapPrompt, baseTools, customTools, mcpProfile
#
# Example:
#
# name: custom-agent
# description: A custom agent for specialized tasks
# systemPrompt: |
#   You are a custom Home OS agent.
#   Focus on the specific domain described here.
# baseTools:
#   - file-tools
# bootstrapPrompt: Acknowledge you are ready.
